services:
  mcp-searxng:
    image: isokoliuk/mcp-searxng:latest
    ports:
      - "8080:8080"
    environment:
      - MCP_HTTP_PORT=8080
      - SEARXNG_URL=https://searxng.example.com
    restart: unless-stopped